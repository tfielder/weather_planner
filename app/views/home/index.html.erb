<h1>Weather Planner</h1>
<h2>A better way to plan your day</h2>

<%= form_for @location do |f| %>
  <%= f.label :location %>:
  <%= f.text_field :location %><br />
  <%= button_to "Submit", results_path(location: @location), class: "button" %>
<% end %>
